<h1>{{title}}</h1>

<div class="row">
  {{#each events}}
    <div class="col s12" style="background-color: {{color}}">
      <a href={{url.public}} style="color:black">
        <span>{{title}}: {{start}}  {{seats_taken}} / {{seats}} taken</span>
      </a>
    </div>
  {{/each}}
</div>
