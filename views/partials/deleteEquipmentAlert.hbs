<div id="deleteEquipmentAlert-{{id}}" class="modal">
  <div class="modal-content">
    <form class="col s21" action="/equipments/{{id}}/delete" method="post">
      <h4>
        Are you sure you want to delete the {{totalForCheckout}} items named {{name}}?
      </h4>
      <div class="modal-footer">
        <a class="modal-close btn waves-effect waves-light pink">Cancel
          <i class="material-icons right"></i>
        </a>
        <button
          class="modal-close btn waves-effect waves-light"
          href="/equipments/{{id}}/delete"
          id="submitDeleteEquipment-{{id}}"
          method="post"
          type="submit"
          name="action"
        >
          Delete
          <i class="material-icons right"></i>
        </button>
      </div>
    </form>
  </div>
</div>
